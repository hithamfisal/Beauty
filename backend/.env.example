PORT=4000
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require
JWT_SECRET=replace_with_a_strong_secret
NODE_ENV=development
CORS_ORIGIN=*
# For local PostgreSQL without SSL only:
# PGSSLMODE=disable
